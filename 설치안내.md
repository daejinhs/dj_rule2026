# 대진고 학생생활규정 개정안 — 최종본 (탭 구조) 설치 안내

대구일중 스타일을 기본으로, **5개 탭**으로 구성한 최종 버전입니다.

| 탭 | 내용 |
|---|---|
| 🏠 안내 | 전부개정안 개요 + **신분 선택**(학생/학부모/교직원 + 직책·학년반) |
| 📌 주요 개정 내용 | 신구대조표 7항목 비교 (정보 제공, 투표 없음) |
| 📖 2026 개정안 전문 | 제1~116조 전문 + 목차, 변경 조문 배지 |
| 📎 원문 자료(PDF) | 개정안 원문·신구대조표·현행(2022) 규정 PDF 미리보기 + 내려받기 |
| ✍️ 의견 제시 | 편(編)별 자유서술 + 개정 동의 체크 + **제출** |

> 메인(안내) 화면에는 개요와 신분 선택만 보이고, 나머지는 탭으로 들어갑니다.
> 제출은 「의견 제시」 탭의 [제출하기] 한 번으로, **신분 + 편별 의견 + 동의**가 한꺼번에 전송됩니다.

구성 파일: `index.html`, `regulation.js`, `Code.gs`, `Admin.html`, `rule-full.pdf`(전부개정안 원문), `rule-compare.pdf`(신구대조표), `rule-2022.pdf`(현행 2022 규정).

## 설치 절차 (요약)

1. **구글 시트 생성**(<https://sheets.new>) → 확장 프로그램 → Apps Script → 이 폴더의 `Code.gs` 붙여넣기
2. Apps Script에서 `Admin.html` 파일을 새로 만들고 이 폴더의 `Admin.html` 내용을 붙여넣기
3. Apps Script의 **프로젝트 설정 → 스크립트 속성**에 `SPREADSHEET_ID`를 추가하고, 응답을 저장할 구글 시트 ID 입력  
   예: 시트 주소가 `https://docs.google.com/spreadsheets/d/1AbC...xyz/edit`이면 `1AbC...xyz`
4. 같은 곳에 `ADMIN_EMAILS`를 추가하고, 관리자 Google 계정을 쉼표로 구분해 입력  
   예: `teacher1@school.kr,teacher2@school.kr`
5. 제출용 배포 생성: **배포 → 새 배포 → 웹 앱** / 실행: 나 / 액세스: **모든 사용자** → 발급 URL 복사
6. `index.html`의 `const GAS_URL = ''` 에 제출용 배포 URL 붙여넣기
7. 관리자용 배포 생성: **배포 → 새 배포 → 웹 앱** / 실행: **웹 앱에 액세스하는 사용자** / 액세스: **Google 계정이 있는 모든 사용자** 또는 학교 도메인 사용자
8. 관리자 계정이 시트를 읽을 수 있도록 Google Sheet 공유 권한을 부여
9. **GitHub Pages**에는 `index.html` + `regulation.js` + `rule-full.pdf` + `rule-compare.pdf` + `rule-2022.pdf`를 업로드
10. 관리자: 관리자용 배포 URL 뒤에 `?view=admin`을 붙여 접속  
   예: `https://script.google.com/macros/s/.../exec?view=admin`

> 기존에 공개 저장소에 올라갔던 Apps Script URL과 관리자 비밀번호는 이미 노출된 것으로 보고, 새 배포 URL로 교체하는 것을 권장합니다.

## 시트 컬럼
제출시각 · 역할 · 세부(직책/학년반) · 개정동의 · 제1편 · 제2편 · 제3편 · 제4편 · 기타의견

## 커스터마이즈
- 7항목 문구: `index.html`의 `QUESTIONS` 배열
- 조문 배지(법령/자체/수정): `index.html`의 `ART_BADGE` 표(조 번호별)
- 규정 전문 갱신: 최신 hwpx로 `regulation.js` 재생성(작업 폴더 `_parse_reg.py`) 후 교체
- 관리자 계정 변경: Apps Script 스크립트 속성의 `ADMIN_EMAILS` 값 수정
